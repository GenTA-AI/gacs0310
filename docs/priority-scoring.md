# Priority Scoring

Priority scoring decides which GACS video job should be generated or refreshed first.

In simple terms:

```text
Higher priority_score = do this video sooner.
Lower priority_score = this can wait.
